# English Translation Status

The locale structure is complete: Dutch documents are in `../nl/`, while graph data and source references remain shared above both locale directories.

The English layer currently covers the navigation, graph model, and the bird-logic end-to-end example. The remaining detailed subsystem pages are intentionally linked to their Dutch originals rather than presented as machine-translated English. They will be translated subsystem by subsystem, preserving source links, ASM ranges, certainty labels, and SVG references.

Priority order for the next English translations:

1. `state-play`, `game-state-machine`, and `state-init`;
2. `alien-logic`, `alien-wave`, and `phoenix-tables`;
3. collision, player, rendering, sound, and platform subsystems.
